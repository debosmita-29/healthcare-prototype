-r requirements.txt

# Hosted demo integrations.
# Keep the local image lean, but install these in docker-compose.hosted.yml
# so mem0 and NeMo Guardrails can be exercised in a production-like stack.
mem0ai==0.1.34
qdrant-client==1.12.1
nemoguardrails==0.11.0
